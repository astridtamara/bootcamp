// @flow
import eventHandlers from '../eventHandlers'

// it('should render state into view', () => {
//     let state = ({name: 'Budi', counter: 0});
//     let newState = eventHandlers.increaseCount(state);
//     expect(newState).toEqual({name: 'Budi', counter: 1});
// })